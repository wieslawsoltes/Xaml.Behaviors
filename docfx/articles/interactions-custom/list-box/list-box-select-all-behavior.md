# ListBoxSelectAllBehavior

Selects all items in the associated `ListBox` when the behavior is attached to the visual tree.

### Usage Example

```xml
<ListBox SelectionMode="Multiple">
    <Interaction.Behaviors>
        <ListBoxSelectAllBehavior />
    </Interaction.Behaviors>
    <!-- Items -->
</ListBox>
```
