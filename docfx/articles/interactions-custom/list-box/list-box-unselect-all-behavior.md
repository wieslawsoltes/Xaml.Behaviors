# ListBoxUnselectAllBehavior

Unselects all items in the associated `ListBox` when the behavior is attached to the visual tree.

### Usage Example

```xml
<ListBox SelectionMode="Multiple">
    <Interaction.Behaviors>
        <ListBoxUnselectAllBehavior />
    </Interaction.Behaviors>
    <!-- Items -->
</ListBox>
```
