# DelayedShowControlAction

Shows a control after a specified delay. Useful for tooltips or delayed feedback.

```xml
<DelayedShowControlAction TargetObject="{Binding #StatusMessage}" 
                          Delay="00:00:02" />
```
