# AsyncLoadBehavior

The `AsyncLoadBehavior` calls an asynchronous method on a target object (or DataContext) when the associated control is loaded.

### Properties
- `MethodName`: The name of the async method to invoke.
- `TargetObject`: The object instance containing the method.
