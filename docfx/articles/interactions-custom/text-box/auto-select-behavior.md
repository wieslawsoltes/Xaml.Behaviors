# AutoSelectBehavior

Automatically selects the entire content of the associated `TextBox` when it is loaded.

## Usage

```xml
<TextBox Text="Select Me">
    <Interaction.Behaviors>
        <AutoSelectBehavior />
    </Interaction.Behaviors>
</TextBox>
```
