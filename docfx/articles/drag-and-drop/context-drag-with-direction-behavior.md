# ContextDragWithDirectionBehavior

This behavior is similar to `ContextDragBehavior` but it also tracks the direction of the drag. This can be useful for custom visual feedback or logic that depends on the drag direction.
